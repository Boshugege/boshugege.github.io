---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getNotes } from "../lib/notes";
import { getAllPosts } from "../lib/content/posts";
import { absoluteUrl, site } from "../lib/site";

const posts = await getAllPosts();
const notes = await getNotes();
const latestNote = notes[0];
const tagCounts = new Map<string, number>();
for (const post of posts) {
  for (const tag of post.tags) tagCounts.set(tag, (tagCounts.get(tag) || 0) + 1);
}
const tags = [...tagCounts.entries()].sort((a, b) => b[1] - a[1] || a[0].localeCompare(b[0]));

function getPostMeta(post: (typeof posts)[number]) {
  return [
    post.date,
    `约 ${post.reading.wordCount.toLocaleString("zh-CN")} 字`,
    `约 ${post.reading.readingMinutes} 分钟读完`,
    post.tags.length > 0 ? post.tags.join(" / ") : "",
  ].filter(Boolean);
}
---

<BaseLayout
  title={site.name}
  canonicalPath="/index.html"
  jsonLd={{
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: site.name,
    url: site.url,
    potentialAction: {
      "@type": "SearchAction",
      target: `${site.url}/index.html?tag={tag}`,
      "query-input": "required name=tag",
    },
  }}
>
  <aside class="directory latest-note-panel" id="latest-note-preview">
    <p class="latest-note-title">最近随想</p>
    <p class="latest-note-text" id="latest-note-text">
      {latestNote ? `${latestNote.date} · ${latestNote.body.replace(/\s+/g, " ").slice(0, 120)}` : "暂无随想"}
    </p>
    <p class="latest-note-link"><a href="/notes.html">查看全部随想</a></p>
  </aside>

  <aside class="directory" id="posts" transition:animate="fade">
    <h2 class="dir-title">文章目录</h2>
    <div class="dir-tools">
      <input class="search-input" type="search" placeholder="搜索全文（标题 / 摘要 / 正文）" aria-label="搜索文章" data-post-search />
      <nav class="tag-nav in-directory" aria-label="标签筛选">
        <a href="/index.html" class="tag top active" data-tag="">全部</a>
        <div class="tags-list collapsed" data-tag-list>
          {tags.map(([tag, count], index) => (
            <a href={`/index.html?tag=${encodeURIComponent(tag)}`} class:list={["tag", { visible: index < 5 }]} data-tag={tag}>
              {tag} ({count})
            </a>
          ))}
        </div>
        {tags.length > 5 && (
          <button class="tag-toggle" type="button" aria-expanded="false" data-tag-toggle data-collapsed-label={`展开全部标签 (${tags.length})`}>
            展开全部标签 ({tags.length})
          </button>
        )}
      </nav>
    </div>
    <div class="dir-list" data-post-list>
      {posts.map((post) => (
        <div class="dir-item">
          <a class="dir-item-title" href={`/${post.url}`} data-astro-prefetch style={`view-transition-name: post-title-${post.slug};`}>
            {post.title}
          </a>
          <span class="meta dir-item-meta" style={`view-transition-name: post-meta-${post.slug};`}>
            {getPostMeta(post).join(" · ")}
          </span>
        </div>
      ))}
    </div>
  </aside>
  <script>
    import "../scripts/home";
  </script>
</BaseLayout>
